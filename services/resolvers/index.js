const resolveFlowHierarchy = require("./resolveFlowHierarchy");
const resolveSubordinatePositions = require("./resolveSubordinatePositions");
const resolveSubordinates = require("./resolveSubordinates");
const resolveScope = require("./resolveScope");
const resolveProductScope = require("./resolveProductScope");

module.exports = {
  resolveFlowHierarchy,
  resolveSubordinatePositions,
  resolveSubordinates,
  resolveScope,
  resolveProductScope,
};